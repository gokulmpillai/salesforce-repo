public with sharing class getConDate {
    // public static List<Contact> getConMonth(Integer month) {
    //     return [SELECT FirstName, LastName, Email, JA_Gender__c, Birthdate FROM Contact];
    // }
}
